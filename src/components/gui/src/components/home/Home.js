import {useEffect} from "react";
import {TextField} from "@mui/material";

export default function Home(){
    useEffect(()=>{

    }, )
    return (
        <div>
            <TextField
                placeholder={"Search"}
                fullWidth={true}
            />
        </div>
    )
}